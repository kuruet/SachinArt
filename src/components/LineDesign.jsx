import React from "react";

const LineDesign = ({ className = "" }) => {
  return (
    <div
    //   className={`line-design ${className}`}
      role="presentation"
      aria-hidden="true"
    />
  );
};

export default LineDesign;
